version: 2.1 
jobs: 
  build-and-test:
    machine:
      image: ubuntu-2204:current
      docker_Layer_caching: true
    steps:
      - checkout
      - run: make deps
      - run: make test
      - run: make docker_build
      - run: docker_push
workflows:
  basic:
    jobs:
      - build-and-test